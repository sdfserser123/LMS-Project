import SignOut from "../../components/auth/signout"

export const StudentDashboard = ({ onLogin }) => {
  return (
    <div>
      <div>
        StudentDashboard
        <SignOut/>
      </div>
    </div>
  );
};